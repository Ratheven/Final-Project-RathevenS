import { useEffect, useState } from "react";

const Homepage = () => {
  //   useEffect(() => {
  //     fetch(`/bestPrice`)
  //       .then((res) => res.json())
  //       .then((data) => {

  //       });
  //   }, []);

  return <></>;
};

export default Homepage;
